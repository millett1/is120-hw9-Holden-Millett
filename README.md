# is120-hw9-Holden-Millett

My first thought for handling this problem was that I wanted to get all the elements in place first. I wanted to put my inputs and my headers and have a very basic page layout so I could visualize how things were happening.

Then I wanted to see if I was able to store the inputs from both the text and the radio. So I started out by using the code we learned in class just to log those inputs. Once I was comfortable logging those inputs, I wanted to create a function that helped me calculate the tip and total based on those inputs.

One thing that tripped me up was how to get a value from a radio input. I was able to look this up and see that you can add a value="" in the html so that was cool to learn. Once I got that it was easier to figure it out. Then building the function took some trial and error, describing to chat exactly what my code said and looked like and what I wanted to happen vs what was actually happening, and then learning from that to change my code around and figure it out until it worked. Once I had the general idea, I was able to build a successful function pretty quickly. Then I just looked up on Chat different ways of rounding and was able to figure out a solid way to round each variable to 2 decimal points. 

Then I played around with styling to make it look a bit more professional and neat and applied some very minimalistic styling. One thing that was cool is that I knew in Python you could do f-strings and I wanted something similar so I was able to look that up and learn how to use `${}` to make it so that the dollar sign shows before the tip and total amounts that change.

In the future, I would like to add additional buttons that can link to useful functions. For example, maybe I could include a back-arrow button that returns the values you just had. I would maybe add a feature so that they can input a custom tip percentage instead of just relying on the four options I gave them. 